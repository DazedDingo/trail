org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# AGP 9.1.0's max *recommended* compileSdk is 36 and it warns above that;
# we are on 37 because flutter_secure_storage 11's AAR metadata demands it.
# Recognised by AGP 9.1 as StringOption.SUPPRESS_UNSUPPORTED_COMPILE_SDK
# ("android.suppressUnsupportedCompileSdk"), matched against the API string,
# so both spellings of API 37 are listed.
android.suppressUnsupportedCompileSdk=37,37.0
